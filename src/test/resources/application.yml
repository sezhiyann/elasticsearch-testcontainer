spring.data.mongodb.uri: mongodb://${mongodb.hostname:localhost}:${mongodb.port:27017}/${mongodb.database:tt-app}



spring:
  elasticsearch:
    host: ${elasticsearch.hostname:localhost}
    port: ${elasticsearch.port:9200}
    uris: http://${elasticsearch.hostname:localhost}:${elasticsearch.port:9200}
    username: ${elasticsearch.username}
    password: ${elasticsearch.password}
